---
title: "Pipeline Reference — Full Step List"
status: "Written 2026-08-31 during the 5-service sediment/coastal rerun, to make sure a long
  multi-tool chain doesn't silently drop a step. Update the Status column as you go; this is a
  living checklist, not a one-time snapshot."
---

**REVERSAL, 2026-09-03**: Becky overrode Steve's retention/protection suggestion — the whole
B-chain below was rerun under the reverted export/risk definition (`R/service_config.R`'s
`SERVICE_AMOUNTS` flipped back to N_export/Sed_export/C_Risk/Pollination/Nature_Access). Rows
below still describe the retention-era run in detail because that architecture/incident history
(the crosswalk fix, the B7 config-centralization work, the docx bug) is real and reusable — but
their **numbers and current-status flags are superseded**. Current real numbers: **189,932**
hotspot cells (was 219,138), **63.3%** attribution gap (was 63.77%), region_wb ranking flipped
back to Latin America & Caribbean leading (1.58×, was East Asia & Pacific 1.20×). See
`docs/manuscript/becky_steve_feedback_plan.md`'s "REVERSAL" section and
`docs/manuscript/paper_draft_5service.qmd`'s own status banner for the full history. Rows updated
inline below where the reversal changed the actual status, not just the numbers.

One row per pipeline stage: what it does, what tool runs it, what it reads and writes, and where
it currently stands. `docs/runbook.md` has the narrative/gotcha detail for each step (Docker flags,
known bugs, partial-rerun rules) — this file is the at-a-glance sequence so nothing gets skipped.

## Legend
✅ done this session · 🔄 running / in progress · ⬜ not started · ⛔ blocked · ⏭️ intentionally
skipped for this rerun (see note)

## A. Raw data → canonical `10k_change_calc.gpkg`

| # | Step | Tool / script | Input | Output | Status |
|---|---|---|---|---|---|
| A1 | Raw zonal extraction (Docker) | `Python_scripts/summary_pipeline_landgrid.py` via Docker (`docs/runbook.md` Step 0) | Raw rasters + `analysis_configs/*.yaml` | `grid_10km_land_synth_zonal_<timestamp>.gpkg` | ⏭️ skipped this round — used an existing intermediate instead (A2) |
| A2 | Sediment + nitrogen raw levels via legacy-grid crosswalk | `scripts/merge_sediment_and_coastal_via_crosswalk.py` | `10k_grid_synth_all.gpkg` (Mar 2026, legacy grid) + `lc_grid_fid_to_master_fid_crosswalk.csv` | `10k_change_calc_DRYRUN_all3.gpkg` | ✅ done, validated, then **corrected and re-run**: found (via B1's LC-driver crash) that 1,609 of 155,131 many-to-one crosswalk groups have a clearly-worse secondary match (4,600-5,900m away) that a plain AVG would blend in; refined to keep only each group's best match(es) before aggregating, re-promoted to canonical. Same fix applied in `hotspot_extraction.qmd`'s own LC-driver join. |
| A3 | Backup current canonical file + hotspot outputs | manual copy | `10k_change_calc.gpkg`, `data/processed/hotspots/`, `data/processed/hotspots_5service/` | `*_BACKUP_2026-08-31.*` | ✅ done |
| A4 | Promote dry-run to canonical | manual rename/copy | `10k_change_calc_DRYRUN_all3.gpkg` | `10k_change_calc.gpkg` (overwritten, 1,522,073 rows confirmed, new columns present) | ✅ done |

## B. Path B (10km grid) downstream — hotspots, stats, attribution

| # | Step | Tool / script | Input | Output | Status |
|---|---|---|---|---|---|
| B1 | Hotspot extraction & export | `analysis/hotspot_extraction.qmd` (`run_hotspot_export: true`) | `10k_change_calc.gpkg`, `HOTS_CFG` | `data/processed/hotspots/**`, `_hotspots_index.csv` | ✅ **rerun 2026-09-03 under the reverted export/risk definition**, clean, 49/49 chunks, no errors. New real number: **189,932** hotspot cells (global pct union), close to the pre-redesign historical baseline (189,927) as a sanity check that the reversal computed correctly. The retention-era note below (219,138 cells, B7/B8 fix bundling) is left for incident history. |
| B2 | Hotspot rasterization | `scripts/gdal_rasterize_hotspots_5service.R` (rewritten 2026-09-01, now reads B1's real output directly) | `data/processed/hotspots/{metric}/global/hotspots_global_{metric}.gpkg` | `data/processed/hotspots_5service/rasters_for_rich/{hotspot_count,N_retention,Sed_retention,C_Prot_service,Pollination,Nature_Access}_{abs,pct}.tif` | ⛔ **stale as of the 2026-09-03 reversal, not yet regenerated — no rush, see C1.** The 5 individual per-service raster names below still say `N_retention`/`Sed_retention`/`C_Prot_service`; the reverted export/risk equivalents (`N_export`/`Sed_export`/`C_Risk`) were never overwritten by the retention redesign in the first place (per the plan doc), so those originals should still be correct and usable — only the aggregate (`hotspot_count`) needs rebuilding against the new 189,932-cell set. Retention-era verification notes below kept for history. |
| B2-old | *(historical, retention era)* | | | | ✅ done, verified: **user decision** — `hotspot_count` + all 5 individual per-service booleans, no water/access split. Confirmed Pollination/Nature_Access source data byte-identical to pre-session (0 differing rows vs. the backup file), but rasterized anyway since no clean end-to-end `hotspot_extraction.qmd` run had ever completed before this session's 2 bug fixes. Sanity-checked: `hotspot_count` range 1-5, per-service columns clean 0/1, 221,372 valid pixels (~1% over the 219,138 vector count, normal reprojection edge effect). Ready for C1. |
| B3 | Spatial clustering & synthesis | `analysis/hotspot_synthesis.qmd` | B1 output | `hotspot_area_stats.csv`, `hotspot_multiservice_stats.csv`, `hotspot_pop_exposure.csv`, `regional_subsets/*` | ✅ **rerun 2026-09-03** under the reverted definition, clean. New real numbers: 189,932 cells (14.6% of evaluated cells), Mangroves C_Risk relative intensity 15.25× (near-identical to the pre-redesign historical 15.25×, a good sign the reversal is faithful), avg-all-5 Mangroves 4.46×; region_wb ranking flipped back to Latin America & Caribbean leading (1.58×) over East Asia & Pacific (1.21×). Retention-era verification notes below kept for history: `hotspot_area_stats.csv` confirmed showing the correct 5-service set (`C_Prot_service, N_retention, Nature_Access, Pollination, Sed_retention`) after fixing a 3rd independent stale-config copy (this qmd recomputes hotspots from its own separate `HOTS_CFG`, not B1's results). **Re-rendered 2026-09-02** (qmd source had been edited by B7's centralization after its last render — output was stale relative to source); numbers reconfirmed identical (Mangroves avg RI 5.27×, `C_Prot_service` 18.35×), so the fix was already correct, just hadn't been re-run. Paper's "Global Hotspot Distribution" section updated with these real numbers (219,138 cells, 16.83%) and its boxplot figure swapped to the current consolidated `boxplots_pct.png` (was pointing at 3 orphaned pre-B8 filenames). **Also redesigned the intensity-chart legend** (`hotspot_relative_intensity_{wwf_biome,income_grp,region_wb,nev_name}.png`, this qmd's `plot-intensity` chunk): dropped the legacy per-group color-swatch legend (invisible `geom_point()` hack, colored each key by group for no reason once bars stopped being colored by group — user/Steve flagged as confusing), replaced with a plain-text numbered key below the chart via a small `patchwork`-stacked panel. Paper's combined 3-in-1 `fig-hotspot-dist` block (map + 2 barcharts) also split into 3 standalone figures — was non-standard and page-break-fragile. **Same legend/title/font fixes extended to the hotspot-magnitude boxplots** (`outputs/plots/boxplots_unified/*/boxplots_{abs,pct}.png`): that generator was moved out of `hotspot_extraction.qmd`'s slow inline chunk into `scripts/mapping/make_hotspot_boxplots.R` (+ `run_hotspot_boxplots.R` standalone entry point reading the cached `plt_long.rds`), both to fix the same legacy legend/title issue and so future styling tweaks don't need a full slow re-render (~90s instead) — see `docs/runbook.md`'s new pattern writeup. Also fixed a real, previously-unnoticed gap found while comparing against the intensity charts: the boxplot's WWF_biome grouping included "Lakes" and "Rock & Ice" as categories, which every other WWF_biome figure in this project explicitly excludes. Also removed the redundant double-caption on 2 single-image paper figures (`fig-boxplots-biome`, `fig-multiplier-compound` — Quarto renders both the image's own alt-text and a trailing div paragraph as separate captions when only one image is present). |
| B4 | Socioeconomic / KS tests | `analysis/KS_tests_hotspots.qmd` | B1/B3 output | KS-test summary tables (feeds paper's Table on hotspot vs. background covariates) | ✅ **rerun 2026-09-03** under the reverted definition, clean. Still 24/25 (96%) significant — same ratio, different services: services are now `N_export, Sed_export, C_Risk, Pollination, Nature_Access`; the one non-significant pairing is now `C_Risk × fields_mehrabi` (p_adj=0.44, CD≈0.001). Sample-size range across the 25 tests: min ~1,000, median ~66,400, max ~68,600. Retention-era note below kept for history: done 2026-09-01, then **rescoped 2026-09-02**: ratios dropped from `include_services` (was `c(service_names(), ratio_names())`, 8 services/40 combinations — same "ratios were never part of the hotspot definition" fix already applied to the boxplots) — now `service_names()` only, 5 services/25 combinations. Also fixed two more hardcoded 8-service `svc_order` copies in this same file (heatmap + Cliff's Delta plot chunks, using the pre-redesign export/risk/ratio names, never sourced from `R/service_config.R`) — same class of bug as the paper's own `tbl-cliffs-delta` table below. Re-run (format switched pdf→html, no LaTeX on this machine, same fix as `hotspot_extraction.qmd`): **24/25 (96%) significant**, one non-significant (C_Prot_service × fields_mehrabi, p_adj=0.10, same decoupling story as before, just one pairing instead of two now that the ratio version isn't tested). |
| B4b | Paper's Cliff's Delta table (`tbl-cliffs-delta`) | inline R chunk in `paper_draft_5service.qmd` | `ks_results_hot_vs_non.csv` | Cliff's Delta table + KS Diagnostics prose | ✅ **re-fixed 2026-09-03** for the reversal — same class of bug, same hardcoded `service_order` vector (this paper doesn't `devtools::load_all()`), flipped back to `c("N_export", "Sed_export", "C_Risk", "Pollination", "Nature_Access")`. Typology prose rewritten again from the real reverted data — genuinely came out cleaner this time: coastal risk stands alone as the higher-development outlier (HDI CD +0.29), the other four cluster together on inequality (GINI CD +0.22 to +0.30). Retention-era note below kept for history: found and fixed 2026-09-02, pre-existing bug: `service_order` was hardcoded to `C_Risk`/`C_Risk_Red_Ratio`/`N_Ret_Ratio`/`Sed_Ret_Ratio`/`Sed_export`/`N_export` — the pre-redesign scheme, **never actually tested under any version of the retention/protection framing**, meaning this table had been silently rendering against the wrong service list (rows just quietly dropped via `filter(service %in% service_order)`, no error) since the redesign, unnoticed because a clean render doesn't flag an empty-looking table as a warning. Fixed to the real 5 services; the whole "typology" prose above the table was also rewritten from the real Cliff's Delta values (old typology grouped services that were never part of this analysis at all) — verified the new pattern doesn't cleanly match the old 3-group narrative (nitrogen and sediment retention diverge from each other, not one shared "intermediate" group like the old ratios were), rewritten to describe what's actually there rather than force the old framing. This paper doesn't `devtools::load_all()` R/service_config.R, so the fix is a hardcoded (but now correct) 5-item vector with a comment — a documented exception to the no-hardcoding rule, not a new violation of it. **Same bug found in 2 more places, same sweep**: the Discussion's "Differentiated Vulnerability Tiers" paragraph (named `N_export`/`Sed_export` explicitly, variables never tested under any version of this framing — rewritten from the real 25-test data, and the "coastal" typology collapsed from a 2-variable range to the single real `C_Prot_service` value) and the Methods' own KS-design description (still said "40 simultaneous tests," "38 of 40," and a sample-size range computed including the now-untested ratio rows — recomputed from the real 25-row data: min ~1,200, median ~52,000, max ~68,600, 24/25 significant). User specifically asked to sweep the whole document for "export" mentions after finding the Discussion instance — the rest of the hits were legitimate (the settled export-as-ratio-input scope, the already-flagged Figure 2 Path A placeholder, accurate InVEST modeling-mechanics description). |
| B5 | Attribution gap (true union) | `scripts/compute_attribution_true_union.R` | B1 output + LC grid crosswalk (Prerequisite) | `lcc_driver_magnitude_summary.csv`, `lcc_es_hotspot_per_driver_risk.csv`, `lcc_es_hotspot_true_union.csv` | ✅ **rerun 2026-09-03** under the reverted definition. New real numbers: **63.29%** attribution gap (36.71% overlap), risk ratio 7.29, odds ratio 10.94 [10.81, 11.08]. Driver-union footprint itself is framework-independent (9.00% of grid, 136,468 cells — unchanged from the retention-era run, as expected since it only depends on land cover data). Per-driver risk ratios shifted: Cropland Expansion now strongest (94.6, was 40.1), Grassland Gain weakest (4.3, was 3.6) — same driver at each end, different magnitude. Retention-era note below kept for history: done, verified — no config drift here (reads B1's output directly, already had the crosswalk many-to-one fix applied from an earlier session). New attribution gap: **63.77%** (36.23% overlap with LCC drivers), risk ratio 8.23, odds ratio 12.34. Old figure was 65.8% — different definition, same ballpark. **Paper text updated 2026-09-02**: found the paper's Annex/Discussion/Conclusions still had the stale 225,113/65.8%/34.2%/7.4/10.79 numbers in ~8 locations (including the per-driver table, whose risk-ratio range itself was stale: old 4.2–97.5 with Urban Expansion strongest, real 3.6–40.1 with Cropland Expansion strongest) — all fixed from the actual CSVs, not by re-deriving. |
| B6 | Native-grid mapping figures | `scripts/mapping/make_global_change_5panel.R` (the actual generator for `global_change_5panel_en.png`, the paper's "Global Pattern of Change" figure) | `10k_change_calc.gpkg` directly (Path B, 10km — **not Path A**, corrected an earlier mischaracterization in this doc and the paper's own placeholder callout) | `outputs/plots/maps/global_change_5panel_{en,es,abs_en,abs_es}.png` (moved 2026-09-02 from `outputs/plots/colombia_report/` — that dir is Colombia-scoped, this figure now also drives the paper's main map; abs variant added same day as a companion showing why SPC is preferred, see paper's Global Pattern of Change section) | ✅ **re-fixed and regenerated 2026-09-03** for the reversal — panel labels/exclusion logic flipped from `C_Prot_service` back to `C_Risk` as the omitted (coastline-fringe, invisible-at-globe-scale) service; panels now show N_export/Sed_export/Pollination/Nature_Access. Note this script also gets wiped by B6b's `unlink()` hazard (see below) — was regenerated a second time same day after that happened, both panel variants confirmed correct after. Retention-era note below kept for history: fixed stale service list (export/risk → retention/protection amounts), regenerated. **User decision 2026-09-01**: coastal protection dropped from this panel — data is correct (49,291 valid cells, full range) but a 1-cell-wide coastline fringe is invisible at full-globe scale next to the other 4 continental-footprint services (confirmed, not a rendering bug). Now a clean 4-panel (2×2) figure; caption updated to note the coastal omission is a visibility choice, not an analysis exclusion. Regional zoom insets for coastal hotspot concentration flagged as a possible future Annex addition, not built. Paper re-rendered. `make_native_change_figure.R` (the export/ratio-paired all-8-service reference figure) still needs a scope decision from the user — pending. `make_colombia_report_maps.R` not yet checked. |
| B6b | Global hotspot count/frequency heatmaps | `scripts/mapping/make_hotspot_count_map.R` (reads B2's canonical gpkg directly, no hardcoded service list — was never one of the 6 config-drift files) | `data/processed/hotspots/{pct,abs}/global/hotspots_global_{pct,abs}.gpkg` | `outputs/plots/maps/{global_hotspot_count_heatmap_*,first_look_map_*}.png` | ✅ **regenerated again 2026-09-03** against the reverted 189,932-cell set. The `unlink()`-wipes-31-files hazard fired again exactly as documented below — recovered everything via `git checkout` (the ones not touched by this reversal) plus a B6 rerun (the two files it actually needed fresh). Retention-era note below kept for history, hazard description still accurate: regenerated 2026-09-02 — found stale (`global_hotspot_count_heatmap_cap4_pct.png` dated **May 6**, predating the entire 5-service redesign; used in the paper's `fig-hotspot-dist`). Script itself wasn't stale, just never re-run. **Hazard, worth knowing before running this script again**: it `unlink()`s every `.png` in `outputs/plots/maps/` before writing its own 8 — wiped 19 unrelated tracked figures (attribution-gap maps, access heatmaps, LCC driver maps, etc.) plus the just-added `global_change_5panel_*` files; all recovered (19 via `git checkout`, 4 via a pre-emptive backup), nothing lost, but back up or `git status` that directory before running it again. |
| B7 | Centralize service config | `R/service_config.R` (`SERVICE_AMOUNTS`, `SERVICE_RATIOS`, `SERVICE_LEGACY_RAW`, `service_canonical_lookup()`, `hotspot_direction_lists()`, `service_names()`, `ratio_names()`) | replaced independently-drifted copies | every consumer now sources this instead of hardcoding | ✅ done, and **paid off exactly as intended on 2026-09-03**: the entire reversal (retention/protection → export/risk) was a 3-way list swap in this one file plus a full B-chain rerun — no consumer needed touching individually. This is the payoff the centralization work was for, even though nobody expected to need it for a reversal specifically. Retention-era incident writeup below kept for history — **6 drifted copies found total**, not the 3 first assumed: `hotspot_extraction.qmd`, `hotspot_synthesis.qmd`, `KS_tests_hotspots.qmd`, `scripts/mapping/make_global_change_5panel.R`, `scripts/extract_hotspots.R` (found via the C1 folder check), and — most consequential — `R/utils_hotspot.R` + `R/hotspot_violins.R`, which are auto-loaded at `devtools::load_all()` time and were **silently shadowing** `hotspot_extraction.qmd`'s own `if (!exists("svc_order"))` guard, meaning that qmd's earlier fix never actually took effect for its bars-section chart. Repo-wide grep sweep run to confirm no more instances in `analysis/`, `scripts/`, `R/` (command: `grep -rln '"N_export"\|"Sed_export"\|"C_Risk"' analysis/ scripts/ R/`). **Known not-yet-audited**: `scripts/audit_hotspot_geography.R` and 7 Colombia/Sandra-deck mapping scripts (`make_attribution_map.R`, `make_colombia_*.R` ×5, `make_faceted_maps.R`, `make_paper_supplement_maps.py`) — not on the current critical path (paper Results, Rich handoff), deliberately deferred rather than silently assumed fine. Re-run the grep above before trusting any of those. See `README.md`/`docs/runbook.md` for the "add a new service" rule this enforces going forward, and memory `project_pipeline_flexibility.md` for the full incident writeup plus the related dashboard/direction-flexibility future-work notes. |
| B8 | Consolidate boxplot outputs | `hotspot_extraction.qmd`'s bars/boxplot chunks | `all_boxplot_services` (from `R/service_config.R`) | `outputs/plots/boxplots_unified/{gc}/boxplots_{abs,pct}.png` (was 4 files: volumetric_abs/pct, ratios_pct, coastal_pct) | ✅ **regenerated 2026-09-03** for the reversal via the fast standalone path (`scripts/mapping/run_hotspot_boxplots.R`, ~90s, reads the cached `plt_long.rds` from B1's rerun) rather than a full slow `hotspot_extraction.qmd` re-render — exactly the use case that script was built for. All 8 files (4 groupings × abs/pct) regenerated clean. Retention-era note below kept for history: code changed (dropped the volumetric/ratio/coastal 3-way split — no longer meaningful now all 5 hotspot-defining services are benefit-framed, and each facet already uses `free_y` so no information is lost), also fixed a related bug this surfaced: the boxplot's own per-service hotspot direction only checked `HOTS_CFG$loss` (5 amounts), leaving the 3 ratios silently misclassified as gain-direction. Rerun pending (bundled with B1's `svc_order` fix below). |

## C. External round-trip (Rich)

| # | Step | Tool / script | Input | Output | Status |
|---|---|---|---|---|---|
| C1 | Send new hotspot rasters to Rich | manual (Drive/Slack), per `docs/hotspots_rasters_data_dictionary.md`-style handoff | B2 + C0 output | shared folder | ⛔ **stale as of the 2026-09-03 reversal — not sent, and shouldn't be until regenerated.** These 18 files reflect the retention/protection 219,138-cell set. Per the plan doc, **no rush**: Rich doesn't need them right now (already has his own output from an earlier round, see C2/Population Exposure section), and the individual per-service export/risk rasters from the original pre-redesign scheme were never overwritten, so those should already be correct. Only the aggregates (`hotspot_count`, `count_water`, `count_access`, `combined_cross`) actually need rebuilding against the reverted 189,932-cell set — this is a real to-do, just not urgent. Retention-era note below kept for history. |
| C0 | Water/access/combined_cross generation | `scripts/extract_hotspots.R` (renamed 2026-09-01 from `extract_hotspots_5service.R` — a 6th independent drifted-config copy found via the C1 folder check; fixed same way as the others) | `plt_long.rds` (from B1) + `10k_change_calc.gpkg` | `data/processed/hotspots_5service/{abs,pct}/global/hotspots_global_5service_{metric}.gpkg` | ⛔ **not yet rerun for the reversal** — same "no rush" status as C1 above, since this feeds it. Retention-era note below kept for history: done, verified: pct n_hot=219,138 (matches B1 exactly), water=108,207, access=126,075, combined_cross=15,144. |
| C2 | Rich's beneficiary/downstream buffer rerun | Rich's own pipeline | C1 rasters | water/access/combined beneficiary rasters | ⬜ external, waiting |
| C3 | Recompile beneficiary exposure | `Python_scripts/extraction_script.py`, `analysis/plot_multiplier_effect.R` | C2 output | `exposure_comparison_compiled.csv`, Figure 9 | ⬜ blocked on C2 |

## D. Path A (300m pixel-level) — NOT reachable this round, but the tool is now identified

| # | Step | Tool / script | Input | Output | Status |
|---|---|---|---|---|---|
| D0 | Identify the Path A generator | — | — | Found 2026-08-31: it's a **separate sibling repo**, `c:\projects\zonal_stats_toolkit` (not part of `global_NCP`) — `process_regional_diff.py`, `compare_and_plot_changes.R`, `compare_paths_bars.R`, configs (`global_ncp_diff_consolidated.ini` etc.), and its own `output_plots_diff/` that mirrors exactly what's copied into `global_NCP/outputs/plots/output_plots_diff/`. This resolves the "generator not found" note from earlier this session. | ✅ found |
| D1 | Path A biome/region/country/income-group aggregation | `zonal_stats_toolkit` (see D0) | raw 300m rasters — the exact requested file list (archived here 2026-09-04 from the now-deleted `rich_slack_message_2026-09-01.draft.md`, since the raw MD5-tagged filenames weren't recorded anywhere else): `global_n_export_tnc_esa{1992,2020}_compressed_md5_{728edc,1d3c17}.tif`, `global_n_retention_ESAmar_{1992,2020}_fertilizer_current_valid_md5_{86031b,82fc1e}.tif`, `global_usle_marine_mod_ESA_{1992,2020}_compressed_md5_{7e68e0,99e715}.tif`, `global_sed_export_marine_mod_ESA_{1992,2020}_compressed_md5_{18eaae,a988c0}.tif`, `realized_polllination_on_ag_ESA{1992,2020}mar_md5_{d3d0d3,da610a}.tif`, `nature_access_lspop2019_ESA{1992,2020}_{compressed_,}md5_{b32b8b,6727ac}.tif` | `output_plots_diff/*_map_data.csv` → copy into `global_NCP/outputs/plots/output_plots_diff/` → Figure 2 + Annex trajectory figures | ⛔ blocked on raw rasters from Rich — **per the plan doc, Rich already uploaded raw 1992/2020 rasters to Drive regardless of the specific request above; verify what actually arrived against this exact file list before assuming the request is still open** |
| D2 | Load-weighted ratio aggregation fix | (extends D1, in `zonal_stats_toolkit`) | raw numerator/denominator per pixel | corrected ratio panels in Figure 2/Annex | ⛔ blocked on D1; also pending the Becky/Steve decision on whether ratios stay in the paper at all |
| D4 | **Reversed by the 2026-09-03 reversal** — the 2026-08-31 scope decision below (drop export/risk panels from Figure 2, since they were "computational inputs only") no longer applies: export/risk are the primary reported variables again. **Figure 2's original paired-row layout (each service's export/risk panel next to its ratio panel) is now the correct target to rebuild, not something to change away from.** Retention-era decision text kept below for history — don't apply it. | `paper_draft_5service.qmd` (Methods text) | already updated to describe export/risk as the primary variables again | Whatever generates the paired-row layout in `zonal_stats_toolkit`/its consuming script should regenerate largely as originally designed once D1 unblocks — re-verify against the current Methods text before rebuilding, don't assume either this row's old framing or an untouched original design is still exactly right. | ✅ scope reversed; rebuild itself still ⬛ blocked on D1 |
| D3 | Paper placeholder callout | `paper_draft_5service.qmd` | — | explicit note that Figure 2/trajectory maps are placeholders pending D1 | ✅ done 2026-08-31 |
| D5 | **Global Pattern of Change map (@fig-global-pattern) — reconsider Path B grid vs. Path A pixel resolution.** Currently deliberately Path B (10km grid, `make_global_change_5panel.R`) — text says "native 10km grid," accurate as of 2026-09-02. Once Path A raw rasters arrive (same D1 request to Rich), regenerate at native 300m pixel resolution for a smoother map; expected to look similar, not a correctness fix. Update the text if/when this happens — don't let it silently drift out of sync with what's actually shown. | `zonal_stats_toolkit` or a new pixel-level version of `make_global_change_5panel.R` | raw 300m rasters (same D1 list) | updated `global_change_5panel_*` at pixel resolution | ⬜ blocked on D1's raw rasters, cosmetic priority |

## E. Paper updates

| # | Step | What | Status |
|---|---|---|---|
| E1 | Remove stale "sediment blocked" language, rewrite Methods for the reverted export/risk framing (Biophysical Modeling, Hotspot Identification exclusion rationale) | text edit | ✅ done 2026-09-03 |
| E2 | Update Results/Discussion/Conclusions/Annex section numbers (hotspot counts, KS tests, attribution gap, regional intensity tables) with real reverted-Path-B output | text edit | ✅ done 2026-09-03 — 189,932 cells, 63.3% gap, region_wb ranking flipped back to Latin America leading, KS typology rewritten from real reverted data |
| E3 | Figure 2 / regional trajectory maps and their caveat callout | — | ✅ callout updated 2026-09-03 to note this figure was never migrated to the retention framing in the first place, so it already reflects export/risk and needs no reversal — still genuinely ⛔ blocked on fresher raw rasters from Rich (D1), a separate, unrelated staleness reason |
| E4 | Ratio-in-paper decision | **Settled by the 2026-09-02 reversal** — ratios are back to secondary/context role (same as always, never hotspot-defining). What's left isn't a decision, it's the load-weighted aggregation bug itself (D2) — still needs fixing whenever Figure 2/Annex ratio panels get rebuilt. Flagged in `docs/manuscript/becky_open_questions_2026-09-03.draft.md`, item 5. | ✅ decision resolved; bug fix still ⬛ blocked on D1 |
| E5 | Re-render paper | `quarto render docs/manuscript/paper_draft_5service.qmd --to html` | ✅ done 2026-09-03, clean, no errors — verified real numbers (189,932; 63.3%; N_export row in Cliff's Delta table) actually appear in the rendered HTML, not just the source. Docx **not** re-rendered — user has manual edits in a docx copy sent to Steve, don't overwrite without checking first (see Section G). |

---

**Rule of thumb for using this file**: update a row's Status the moment a step finishes or a
decision changes — don't batch it for later, that's exactly how a step gets lost. If a new
dependency turns up mid-run (like C1 did today), add a row rather than mentioning it only in chat.

## F. Deferred cleanup (flagged 2026-09-01, not started)

**Old output clutter**: after this rerun settles, `outputs/plots/` and `data/processed/hotspots*`
almost certainly have stale files from the 8-service and July-28 water/access eras sitting
alongside the current ones (the boxplot consolidation in B8 alone drops 2 of 4 filenames per
grouping, e.g. `boxplots_volumetric_abs.png` etc. become orphaned once the new `boxplots_abs.png`
exists). Needs a deliberate review pass — what's safe to delete vs. what's still a real reference
point (e.g. `data/processed/hotspots_5service/rasters_old/` was deliberately kept once already as a
"don't confuse with this" marker) — not a blind `rm`. Do this after the current rerun is fully
verified and paper Results are updated, not before.

## G. Docx export for Steve (2026-09-02) — a real Quarto/environment bug, not a paper problem

Rendering `paper_draft_5service.qmd` to docx (`quarto render ... --to docx`) for Steve (who wants
Word, not qmd/html) surfaced a serious, silent bug in this Quarto install (1.9.37): **every
`.callout-*` div's content vanishes entirely in docx output** — not just unstyled, completely
absent from the document. Confirmed via a minimal 4-line isolated test file (both 3-colon and
4-colon fence syntax), so this is an environment/Quarto-version issue, not something specific to
this paper's markup. Verified by round-tripping the docx back through pandoc to plain text and
diffing word counts against the html version (~1,000-word gap) — don't trust a clean `quarto
render` exit code alone to mean the docx is complete; it renders with no warning either way.

**Fixed** by converting all 16 real callout divs (the other ~6 `:::`-fenced blocks in this file are
figure-panel divs, left alone) to plain blockquotes (`> **Label:** text`), which don't depend on
Quarto's callout Lua filter at all — guaranteed to survive in every pandoc output format. Script
used: a one-off Python pass (not saved to the repo, per the usual scratch-script convention),
converting `:::: {.callout-TYPE}\n...\n::::` blocks into `> **Type:** ...` with each line prefixed
`> `. Also found two more docx-specific gaps in the same pass: (1) the three `DT::datatable()`
tables degrade to a raw ASCII-table dump in docx (`tbl-cliffs-delta` was already plain `kable()`
and fine; `tbl-attribution-drivers` and `tbl-regional-intensity` switched to `kable()` too — also
fixed real stale data in the latter while in there, see below); (2) Quarto's docx writer doesn't
render YAML `title`/`subtitle`/`author` as visible body text the way html does — the document
opened with nothing on page 1. Fixed with a `{.content-visible when-format="docx"}` block (this
mechanism, unlike callouts, was confirmed working) holding a manual, `.unnumbered` title block, so
it doesn't duplicate or get numbered into the section sequence, and doesn't appear in html (which
already gets its title from the YAML normally).

**Also found in the same pass, unrelated to docx**: `tbl-regional-intensity` (World Bank Region
relative intensity, Annex) was hardcoded to 5 stale regions with **Latin America ranked above East
Asia & Pacific** — the real, current (at the time — retention-era) `hotspot_area_stats.csv` had all
7 regions with East Asia & Pacific actually leading (1.20× vs Latin America's 1.11×). Recomputed
and fixed. **Superseded by the 2026-09-03 reversal**: under the reverted export/risk definition,
the ranking flipped back to Latin America & Caribbean leading (1.58×) over East Asia & Pacific
(1.21×) — both rankings were genuinely correct for their respective service definitions, not a
repeat of this bug. Don't "fix" it back without checking which definition is actually current.

**How to apply, if this file (or any future docx-targeted qmd in this project) needs edited
again**: don't reintroduce `.callout-*` divs for anything that must survive in docx — use the
blockquote convention above instead. Re-verify any docx render by round-tripping through pandoc
(`pandoc file.docx -t plain`) and checking word count against the html version, not just a clean
exit code.
