% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iterate_lcc_metrics.R, R/utils_lcc_metrics.R
\name{iterate_lcc_metrics}
\alias{iterate_lcc_metrics}
\title{Iterate zonal land cover change extraction over year steps}
\usage{
iterate_lcc_metrics(
  raster_list,
  polygons,
  id_col = "id",
  percent = TRUE,
  verbose = TRUE,
  mc = FALSE,
  ncores = NULL,
  digits = 0
)

iterate_lcc_metrics(
  raster_list,
  polygons,
  id_col = "id",
  percent = TRUE,
  verbose = TRUE,
  mc = FALSE,
  ncores = NULL,
  digits = 0
)
}
\arguments{
\item{raster_list}{A named list of SpatRaster layers, where names correspond to years (e.g., "1992", "1995", ...).}

\item{polygons}{An sf object with the spatial units.}

\item{id_col}{Name of the unique identifier column in \code{polygons}.}

\item{percent}{Logical. If TRUE, values are in percentage.}

\item{verbose}{Logical. If TRUE, prints progress.}

\item{mc}{Logical. If TRUE, uses parallel::mclapply for parallel execution.}

\item{ncores}{Integer. Number of cores to use if \code{mc = TRUE}. Default is NULL.}

\item{digits}{Integer. Number of decimal digits to keep in the change metrics (passed to diffTablej). Default is 0.}
}
\value{
A single data.frame with land cover change metrics for all year steps and polygon units.

A single data.frame with land cover change metrics for all year steps and polygon units.
}
\description{
Wraps extract_lcc_metrics() over multiple pairs of SpatRaster objects and returns combined results.

Wraps extract_lcc_metrics() over multiple pairs of SpatRaster objects and returns combined results.
}
\examples{
# iterate_lcc_metrics(raster_list = list("1992" = r1992, "1995" = r1995, ...),
#                    polygons = basins_sf,
#                    id_col = "id")

# iterate_lcc_metrics(raster_list = list("1992" = r1992, "1995" = r1995, ...),
#                    polygons = basins_sf,
#                    id_col = "id")

}
