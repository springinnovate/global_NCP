# CONTEXT: resuming a fresh session — 2026-08-19

Paste this whole file into a fresh Claude Code session in `c:\projects\global_NCP`
(same workspace also has `c:\projects\LC_orinoquia` and `c:\projects\zonal_stats_toolkit`
as siblings — keep them there, don't remove).

## Immediate state: Sandra Valenzuela deck (WWF Colombia CEO meeting, Friday)

`docs/presentations/sandra_valenzuela_colombia_case.qmd` — actively being polished, in good
shape, 10 slides. Render with:
```
cd C:\projects\global_NCP
quarto render docs\presentations\sandra_valenzuela_colombia_case.qmd
```
(If you get "No valid input files passed to render," you're not in the repo root — `cd` there
first or use the full path.) **The `.pptx` output may fail with a file-lock error if the user
has the pptx open in PowerPoint** — if so, render `--to revealjs` only to keep iterating, ask the
user to close PowerPoint before a full-format render.

Done so far this session: title reframed, combined WWF/NatCap/NatCap-TEEMs logo + standalone
InVEST logo (per its trademark policy, not merged into the combined one), systemic map-stretch
bug fixed, dynamic `date: last-modified`, reworked priority-overlap map (intensity-shaded not
binary — Orinoquía's "critical" cells are real but shown as disproportionately marginal on the
old binary map, confirmed empirically: 66.8% barely-above-threshold vs 25.1% elsewhere), a new
slide-2 background collage (6-panel global change maps, tuned through several rounds to
`width:123%; height:102%` centered, opacity 0.5 — this uses a **plain positioned `<img>`, not
reveal.js's own `background-image` slide attribute**, because that attribute has an unexplained
rendering bug in this quarto/revealjs version that silently drops content — don't go back to it),
and a **new slide splitting the old opener in two**: a NatCap-Alliance/WWF-Global-Science intro
slide (CONVEI, NatCap TEEMs, Conservation Navigator, Roadmap 2030 — all verified against official
WWF/NatCap sources, sources logged in that slide's speaker notes) followed by the original
"enfoque global" pipeline-pitch slide, trimmed to not repeat the partner list twice.

**Rendered 2026-08-19 (revealjs only, clean, no errors)**: an expanded methodology explanation
in the "Oportunidad: activos naturales críticos" slide's notes — what critical natural assets
are, why they matter (30% of land sustains 90% of the 12 local NCPs; 44% including 2 global NCPs),
and exactly how Chaplin-Kramer et al. 2022 computed them (`prioritizr` R package, 14 NCP layers by
Neugarten & Chaplin-Kramer, Eckert IV equal-area grid, priority rank 1-20 via repeated-budget
optimization). All verified 2026-08-19 against nature.com/articles/s41559-022-01934-5 and the
paper's OSF data repo (`osf.io/r5xz7`) — not guessed.

## DONE 2026-08-19: individual critical-natural-assets rasters downloaded

All 10 "realized" per-service rasters from OSF project `r5xz7` (folder `data/NCP layers/`) are
now in `data/external/critical_natural_assets/individual_layers/` (~585MB total, `data/` is
gitignored so this is safe/untracked): sediment deposition (500km & 50km attenuation), coastal
protection, nitrogen retention (500km & 50km), nature access (urban/rural × 60min/360min),
pollination.

Integrity note: each filename carries an `md5_...` tag, but 6 of the 10 don't match the actual
downloaded file's md5 — **this is expected, not corruption**. Confirmed via OSF's own
`X-Waterbutler-Metadata` (the platform's authoritative hash for the file) that the real download
matches perfectly; the filename's md5 tag is a stale identifier from an earlier stage of the
paper's own processing pipeline, not a live checksum. Don't re-flag this as a download problem
if it comes up again.

Still not downloaded (large, low-priority, only pull if asked): `potential service layers`
subfolder on OSF — `potential_sedimentdeposition` (~3GB), `potential_nitrogenretention` (~2.2GB),
`cv_service_sum` (~72MB) — theoretical-max versions, not "realized"/current.

Not yet done: nothing uses these new individual layers yet (no script/report references them) —
ask the user what they want built with them before doing anything further.

The OSF project README (`data/README.md` on OSF, fetched 2026-08-19) documents the full
data-prep/prioritization pipeline (`prioritizr`, Eckert IV grid at 1/5/10/100km, per-country +
global features) if deeper methodology detail is ever needed.

## Other threads (unchanged since 2026-08-18, see memory for full detail — this is just a pointer)

- **Paper edits** (Becky/Steve feedback): ~half done, paused. Becky unavailable this week per the
  user, so lower urgency for now, but "send corrected version ASAP" once resumed.
- **`docs/presentations/presentation.qmd`** (general internal deck, separate from Sandra's): still
  needs the same 5-service-redesign + Becky-feedback sync. Untouched.
- **SWY model integration**: active, priority #2. CN lookup table milestone reached; mangrove/
  flooded-grassland biome patch and global-run-vs-mosaic scale question still open. Next step:
  message Rich. Full detail in memory `project_swy_model_integration.md`.
- **LC_orinoquia** (dissertation, personal laptop): Phase 1 blocked — Piedmont data lived on Temple
  OneDrive, access revoked post-graduation, user checking recovery options. See
  `LC_orinoquia/docs/HANDOFF_2026-08-17_cenipalma.md` for full detail (also covers a separate
  thread: presenting at Cenipalma's Seminario Científico, 2026-09-02, with an agreed political-
  sensitivity framing — read that doc before touching anything Cenipalma-related).
- **NatCap tools crash course**: new, deliberately mid-term/deferred idea (Quarto book on
  InVEST/RIOS/OPAL/ROOT/MESH in a new sandbox repo under `C:\projects\`, not yet created) — see
  memory `project_natcap_tools_crash_course.md`. Don't start building unprompted.

## DONE 2026-08-19 (later): Becky's per-service critical-asset idea — data staged

From a meeting with Becky (per user's Granola notes): cross decline hotspots with **per-service**
(not just aggregate) critical natural asset maps. Full detail + confirmed OSF letter-code mapping
in memory `project_becky_per_service_cna_idea.md`. Status: **data is now staged, no analysis
built yet.**

- Found the OSF `solutions` folder (`r5xz7`), read its `Readme.pdf`/`solutions.csv`, confirmed
  Colombia (`col`) 90%-target single-service solutions exist for the 5 services already in the
  Sandra deck: coastal risk reduction (`S`), nature access 1hr (`Z`/E1), nitrogen retention 500km
  (`A1`/F1), pollination (`B1`), sediment retention 500km (`D1`/G1).
- Downloaded the full `solutions.zip` (875MB, all countries+scenarios — OSF doesn't expose
  individual-file download), extracted just the 5 Colombia tifs, deleted the zip.
- **Now live in the repo**: `data/external/critical_natural_assets/per_service_solutions/`
  (5 small tifs, ~900KB each, + a README mapping filenames to letter codes/services).
  `data/` is gitignored so these are untracked, same as the other new rasters.
- **Not yet done — next step when resuming**: nothing uses these yet. Design + build the
  overlap analysis (crossing Colombia decline hotspots against these 5 per-service solutions)
  once the user confirms scope — likely extending
  `scripts/mapping/make_colombia_priority_overlap.R` or a sibling script, same "≥50% majority
  rule" convention already used for the aggregate overlap map. Don't build unprompted.
- This is distinct from the "realized" magnitude layers downloaded earlier the same session
  (`individual_layers/`) — see the memory file for the full distinction, don't conflate. Also
  distinct from the aggregate `local_NCP_all_targets/` raster already in the repo.

## Working notes for this user

Evidence over guessing — verify claims (data sources, org relationships, citations) against real
sources before they go in front of stakeholders; this session caught and fixed several
inaccuracies this way (WWF's actual NatCap Alliance role, Becky's real title vs. "my supervisor,"
a map caption bug that silently flipped 14.2% to 4.2%). Never commit without being asked
explicitly. Render and visually screenshot changes before claiming something is fixed — several
bugs this session were invisible without an actual rendered screenshot.
