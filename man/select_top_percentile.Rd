% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantile_selectR.R
\name{select_top_percentile}
\alias{select_top_percentile}
\title{Select Top Percentile of Raster Values}
\usage{
select_top_percentile(raster, percentile = 0.9)
}
\arguments{
\item{raster}{A \code{SpatRaster} object from the \code{terra} package.}

\item{percentile}{A numeric value between 0 and 1 indicating the percentile threshold.
For example, \code{0.9} retains the top 10\% of values.}
}
\value{
A \code{SpatRaster} object with the same structure as the input raster. For each band,
only the pixels above the specified percentile threshold are retained; others are set to NA.
}
\description{
This function selects the top percentile of raster values, retaining only pixels
with values greater than or equal to the given percentile threshold. It works for
both single-band and multi-band \code{SpatRaster} objects.
}
\examples{
library(terra)
r <- rast(ncol=10, nrow=10)
values(r) <- runif(ncell(r), min=0, max=100)
top_10 <- select_top_percentile(r, percentile=0.9)

# For multi-band rasters
multi_r <- c(r, r * 2)
top_multi <- select_top_percentile(multi_r, percentile=0.8)

}
