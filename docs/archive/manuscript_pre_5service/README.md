**Archived 2026-09-15.** These are the pre-5-service-redesign paper materials, superseded by
`docs/manuscript/paper_draft_5service.qmd` (a real redesign, not a rename — different title,
different hotspot definition). Kept for the record, not deleted:

- `paper_draft.qmd` / `.html` / `.docx` — "Global Ecosystem Service Time Series: A Spatiotemporal
  Analysis of Change, Exposure, and Attribution," the original draft circulated for advisor
  feedback.
- `index.tex` — a LaTeX export of the same original draft.
- `workflow.qmd` / `.html` — a standalone rendering of the pipeline workflow diagram; the current
  paper embeds this same diagram directly (`fig-workflow` in `paper_draft_5service.qmd`) rather
  than as a separate file.
