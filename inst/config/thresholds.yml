mode: percent        # or: count
pct_cutoff: 0.05
count_n: null        # used if mode=count
side: both           # used when rule_mode=explicit (high/low/both)
rule_mode: vectors   # or: explicit
trim_x: [0.01, 0.99]
trim_y: [0.01, 0.99]
