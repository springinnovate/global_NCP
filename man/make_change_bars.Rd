% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/change_bars.R
\name{make_change_bars}
\alias{make_change_bars}
\title{Bar charts of trimmed change by group}
\usage{
make_change_bars(
  group_col,
  stub,
  svc_order,
  metric = c("pct", "abs"),
  include_global = FALSE,
  svc_order_only = TRUE,
  sort_bars = TRUE,
  cut_q = 0.999,
  out_dir = NULL,
  save = TRUE,
  show = FALSE,
  title_prefix = NULL
)
}
\arguments{
\item{group_col}{Character scalar naming the grouping column (e.g., "region_wb").}

\item{stub}{File name stub for PNGs.}

\item{svc_order}{Character vector giving the facet order (only these are shown
by default; set \code{keep_only_ordered = FALSE} inside your function if you want extras).}

\item{include_global}{Logical; include a “Global” bar in each facet.}

\item{sort_bars}{Logical; order x-bars within each facet by \code{sort_stat}.}

\item{out_dir}{Output directory for PNGs.}

\item{save}{Logical; write PNGs.}

\item{show}{Logical; print plots to device.}

\item{keep_only_ordered}{Logical; if TRUE (default in your function), hide services
not in \code{svc_order}.}

\item{sort_stat}{One of \code{"abs_mean"} or \code{"pct_mean"}.}

\item{sort_desc}{Logical; descending order if TRUE.}

\item{file_tag}{Optional suffix for filenames (helps avoid overwrites).}

\item{width, height, dpi}{PNG settings.}

\item{pin_global}{Logical; if TRUE, place "Global" first on x-axis when present.}
}
\value{
(invisibly) a list with \code{abs_plot}, \code{pct_plot}, and the \code{data} used.
}
\description{
Creates vertical bar plots of trimmed mean |absolute change| and |percent change|
for each service, faceted in the order you supply via \code{svc_order}. X-axis bars can
be sorted by magnitude within each facet. Optionally includes a “Global” bar.
}
