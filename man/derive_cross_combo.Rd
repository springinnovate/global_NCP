% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_hotspots.R
\name{derive_cross_combo}
\alias{derive_cross_combo}
\title{Derive a cross-category "AND" column from two or more combo count columns}
\usage{
derive_cross_combo(data, combo_names, new_col = NULL)
}
\arguments{
\item{data}{A data frame containing one \verb{count_<name>} column per combo
named in \code{combo_names}, as produced by \code{extract_hotspots(..., combos = )}.}

\item{combo_names}{Character vector of two or more combo names — the same
keys used in the \code{combos} list passed to \code{extract_hotspots()} (without
the \code{count_} prefix). The derived column is \code{1} where \emph{every} named combo
has at least one hotspot service present in that cell, \code{0} otherwise.}

\item{new_col}{Name for the derived column. Defaults to \code{combo_names}
pasted together with \code{"_x_"} (e.g. \code{"water_x_access"}).}
}
\value{
\code{data} with one additional integer (0/1) column, \code{new_col}.
}
\description{
\code{extract_hotspots()}'s \code{combos} argument produces one \verb{count_<name>} column
per named service group — "how many of this group's services are hotspots
in this cell." That mechanism only ever counts \emph{within} one named group; it
has no built-in way to ask "is this cell a hotspot in combo A AND also in
combo B" across two different combos. This helper adds that one derived
step so it doesn't have to be re-derived by hand per case — see the
\code{combined_cross} column in \code{scripts/extract_hotspots_5service.R}, which
wrote this exact pattern out manually before this function existed.
}
\examples{
\dontrun{
hs <- extract_hotspots(
  df = plt_long, value_col = "pct_chg",
  loss_services = c("Nature_Access", "Pollination"),
  gain_services = c("Sed_export", "N_export", "C_Risk"),
  combos = list(
    water  = c("N_export", "Sed_export"),
    access = c("Nature_Access", "Pollination", "C_Risk")
  ),
  id_cols = c("fid")
)
derive_cross_combo(hs$summary_df, c("water", "access"), new_col = "combined_cross")
}

}
\seealso{
\code{\link[=extract_hotspots]{extract_hotspots()}}
}
