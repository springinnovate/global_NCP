% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_hotspots.R
\name{filter_multidim}
\alias{filter_multidim}
\title{Filter a data frame by multiple geographic/socioeconomic dimensions}
\usage{
filter_multidim(
  data,
  region_wb = NULL,
  income_grp = NULL,
  WWF_biome = NULL,
  country = NULL
)
}
\arguments{
\item{data}{A data frame containing any subset of \code{region_wb}, \code{income_grp},
\code{WWF_biome}, and \code{nev_name} columns.}

\item{region_wb}{Character vector; keep only rows where \code{region_wb} is in
this set. \code{NULL} = no filter.}

\item{income_grp}{Character vector; keep rows matching these income groups.}

\item{WWF_biome}{Character vector; keep rows matching these biomes.}

\item{country}{Character vector; matched against the \code{nev_name} column.}
}
\value{
The filtered data frame (same class as \code{data}).
}
\description{
Applies any combination of region, income group, biome, and country filters
to \code{data}. Only criteria that are non-NULL are applied, so partial
specifications work naturally. Designed for \code{plt_long} or the raw grid
\code{sf} object where all grouping columns are present as attributes.
}
\details{
For cross-cuts not available in the pre-computed summary CSVs (e.g.,
"Sub-Saharan Africa + Low income"), apply this to the raw grid data first,
then call \code{\link[=extract_hotspots]{extract_hotspots()}} or \code{agg_by_group()} on the result.
}
\examples{
\dontrun{
# Single dimension
ssa <- filter_multidim(plt_long, region_wb = "Sub-Saharan Africa")

# Cross-cut: Sub-Saharan Africa + Low income
ssa_li <- filter_multidim(plt_long,
                          region_wb  = "Sub-Saharan Africa",
                          income_grp = "5. Low income")
}

}
\seealso{
\code{\link[=extract_hotspots_by]{extract_hotspots_by()}}
}
