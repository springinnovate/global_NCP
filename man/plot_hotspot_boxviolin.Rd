% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hotpot_boxviolin.R
\name{plot_hotspot_boxviolin}
\alias{plot_hotspot_boxviolin}
\title{Faceted hotspot vs non-hotspot box/violin overlays}
\usage{
plot_hotspot_boxviolin(
  plt_long,
  inverse_df,
  socio_labels,
  drop_services = NULL,
  trim_p = c(0.01, 0.99),
  y_trans = c("identity", "log10", "sqrt", "log1p")[1],
  show_violin = TRUE,
  show_box = TRUE,
  show_jitter = FALSE,
  violin_trim = TRUE,
  jitter_frac_nonhotspot = 0.02,
  jitter_max_hotspot = 2000,
  width = 10,
  height = 6,
  dpi = 300,
  out_dir = "output_charts",
  filename_suffix = "_boxviolin.png",
  run_id = NULL,
  use_ragg = TRUE,
  hide_inline = TRUE,
  print_interactive = TRUE
)
}
\arguments{
\item{plt_long}{Hotspot rows (e.g., top/bottom thresholded subset).}

\item{inverse_df}{Complement rows (non-hotspots).}

\item{socio_labels}{Named character vector: names are variables to plot; values are facet labels.}

\item{drop_services}{Character vector of services to exclude.}

\item{trim_p}{Length-2 numeric, pct_chg quantile trim per service, e.g. c(0.01, 0.99).}

\item{y_trans}{Optional transform for y: "identity","log10","sqrt","log1p".}

\item{show_violin, show_box, show_jitter}{Logical toggles for layers.}

\item{violin_trim}{Logical; if TRUE, trim violin tails.}

\item{jitter_frac_nonhotspot}{Numeric in \link{0,1}, fraction of non-hotspot rows to jitter per facet.}

\item{jitter_max_hotspot}{Optional cap on hotspot jitter points per facet (NULL = all).}

\item{width, height, dpi}{Figure size for ggsave.}

\item{out_dir}{Output directory.}

\item{filename_suffix}{File suffix.}

\item{run_id}{Optional string stamped into filename and caption.}

\item{use_ragg}{Logical; if TRUE, use ragg::agg_png for reliability.}

\item{hide_inline}{Logical; hide inline plots when knitting.}

\item{print_interactive}{Logical; print plots when interactive and not knitting.}
}
\value{
Invisibly, vector of written file paths.
}
\description{
For each service, facet by socio variable and compare hotspot vs non-hotspot
distributions using violin and/or boxplot pairs, with optional jitter samples.
}
